const imgId = 'imageid';
const avatarId = 'avatarid';
const blobId = 'blobid';

export const info = {
    imgUrl: `mxc://example.com/${imgId}`,
    mediaId: imgId,
    mediaName: 'my-media-file.jpg',
    avatarUrl: `mxc://example.com/${avatarId}`,
    avatarId,
    blobUrl: `mxc://example.com/${blobId}`,
    blobId,
    blobName: 'Some data without ext.',
    maxTs: 1583155366238,
};
